# frozen_string_literal: true

require 'spec_helper'

describe '<%= name %>' do # rubocop:disable RSpec/DescribeClass
  let(:title) { 'namevar' }
  let(:params) do
    {}
  end

  on_supported_os.each do |os, os_facts|
    context "when on #{os}" do
      let(:facts) { os_facts }

      it { is_expected.to compile }
    end
  end
end
