# See https://docs.puppet.com/puppet/4.5/reference/lang_write_functions_in_puppet.html
# for more information on native puppet functions.
function <%= name %>(Integer $value) >> Integer {
    unless $value {
      return 0 
    }
    $value * $value
}
